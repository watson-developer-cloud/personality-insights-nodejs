---
declared-services:
  my-pi-service:
    label: personality_insights
    plan: tiered

applications:
- name: personality-insights-livedemo
  command: npm start
  path: .
  memory: 512M
  instances: 1
  services:
  - my-pi-service
